'use strict';

var React = require('react');

var Collect = React.createClass({

    render: function() {
        return (
            <h1>Collect data.</h1>
        );
    }

});

module.exports = Collect;
